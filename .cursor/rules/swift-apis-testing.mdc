---
description: Swift API 设计、可测试性与文档——公共 API、依赖注入、注释与测试
globs: "**/*.swift"
alwaysApply: false
---

# Swift API 与可测试性规则

## API 设计

- 公共 API 命名清晰、参数顺序合理；使用默认参数与重载减少调用方负担。
- 使用 `internal`、`private`、`fileprivate` 控制可见性；对外只暴露必要类型与方法。
- 采用协议抽象依赖（如 Repository、NetworkService），便于替换实现与测试。

## 依赖与可测试性

- 通过初始化注入依赖（构造注入），避免在类型内部直接 `Service.shared` 或硬编码实现。
- 协议 + 默认实现或具体类型均可；测试时注入 Mock/Stub 实现。
- 避免在业务类型中直接依赖全局单例或静态方法，除非是明确的跨层基础设施（如日志）。

## 文档与注释

- 公共 API 使用文档注释（`///` 或 `/** */`），简要说明用途、参数、返回值与可能抛出的错误。
- 复杂业务逻辑或非显然的约束在代码内用简短注释说明“为什么”，而非重复“做什么”。
- 标记 `TODO`/`FIXME` 时尽量附带上下文或 issue 引用。

## 测试与构建

- 单元测试覆盖核心逻辑与边界情况；异步逻辑使用 `async` 测试或 expectation。
- 不依赖未注入的外部资源（网络、数据库）的测试更稳定；必要时用协议 + Mock。
- 修改后确保项目能通过 `xcodebuild` 或 SPM `swift build`；注意 Swift 与 Xcode 版本兼容性。
